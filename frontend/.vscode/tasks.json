{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run generateJson",
            "type": "shell",
            "command": "node generateJson.js",
            "problemMatcher": [],
            "runOptions": { "runOn": "folderOpen" }
        }
    ]
}
