# DM（数値地形図データ）→ GeoJSON 変換

## 概要

`dm.ts` は、国土交通省の「公共測量標準図式 数値地形図データファイル仕様」に基づく DM ファイルを GeoJSON に変換するライブラリです。**旧DM** と **拡張DM** の両フォーマットに対応しています。

## DM ファイルの基本仕様

- **固定長 84 バイト/レコード**（Shift-JIS エンコーディング、改行 CR+LF）
- **座標系**: 平面直角座標系（X = 北方向、Y = 東方向）→ WGS84 経緯度に変換
- **系番号**: 1〜19（日本全国を19の系に分割、測量法施行令 第2条）

## 旧DM と拡張DM の違い

| 項目 | 旧DM | 拡張DM |
|------|------|--------|
| インデックス | `I ` レコードに座標系番号 | I レコードなし |
| 要素レコード | `F ` + pos17 にデータタイプ | `E` + データタイプ1桁（`E1`〜`E7`） |
| 座標レコード | `D2` / `D3` プレフィックス付き | プレフィックスなし（スペースまたは数字で開始） |
| 座標フィールド幅 | 10バイト × 最大4ペア/行 | 7バイト × 最大6ペア/行 |
| 座標単位 | mapLevel に依存（mm/cm/m） | 図郭座標行の座標値単位フィールドに依存（mm/cm/m） |
| 座標変換 | 生値 → `toMeters()` → 絶対座標 | 図郭原点(m) + 座標値 / coordUnitDivisor |

### フォーマット自動判定

ファイル内に `E1`〜`E7` で始まるレコードがあれば拡張DM、`F ` や `I ` レコードがあれば旧DMと判定します。

## レコード構成

### 共通レコード

| レコード | 先頭 | 内容 |
|----------|------|------|
| 図郭 (a) | `M ` | 図郭識別番号、名称、地図情報レベル |
| 図郭 (b) | 空白開始 | 左下/右上の図郭座標（M直後の行） |
| グループヘッダ | `H ` | 分類コード、要素数 |

### 旧DM 固有レコード

| レコード | 先頭 | 内容 |
|----------|------|------|
| インデックス | `I ` | 座標系番号、計画機関名 |
| 要素 | `F ` | 分類コード、データタイプ、座標数 |
| 2D座標 | `D2` | X,Y ペア（10桁 × 最大4組） |
| 3D座標 | `D3` | X,Y,Z（10桁 × 最大2組） |
| 注記 | `L ` | テキスト、座標、角度、文字高さ |

### 拡張DM 固有レコード

#### E レコード（要素レコード）

```
位置   長さ  内容
[1]    1    "E" 固定
[2]    1    データタイプ (1=面, 2=線, 3=円, 4=円弧, 5=点, 6=方向, 7=注記)
[3-6]  4    分類コード
[7]    1    空白
[8]    1    実データ区分
[9-12] 4    間断区分
[13-16] 4   要素識別番号
[17]   1    精度区分
[18]   1    予備
[19-23] 5   地図情報レベルコード
[24-27] 4   フラグ等
[28-31] 4   座標数
[32-35] 4   座標レコード数
[36-42] 7   X座標（点・注記で埋込時）
[43-49] 7   Y座標（点・注記で埋込時）
```

**例**: `E22101 0   0   1 2 02380 01   2   1`
- `E` = 要素レコード
- `2` = 線データ
- `2101` = 分類コード（都府県界）
- 要素ID = 1、座標数 = 2、レコード数 = 1

#### 座標レコード（E レコードの次行）

```
7バイト × 12値 = 6座標ペア (X, Y) / 行
位置    内容
[1-7]   X1
[8-14]  Y1
[15-21] X2
[22-28] Y2
  ...
[71-77] X6
[78-84] Y6
```

座標値 `0` はパディング（無効値）。座標値の単位は図郭座標行の座標値単位フィールドにより決まります（下記参照）。

**例**: ` 245045 232912 251716 271023      0      0`（座標値単位=10(cm)の場合）
- ペア1: X=2450.45m, Y=2329.12m
- ペア2: X=2517.16m, Y=2710.23m

## 座標変換の流れ

### 旧DM

```
生の座標値
  → toMeters(value, mapLevel)  // mm/cm/m → メートル
  → jpRectToWgs84(x_m, y_m, zone)  // 平面直角 → WGS84
  → [lon, lat]
```

### 拡張DM

```
生の座標値
  → 図郭原点X(m) + 座標値 / coordUnitDivisor  // 絶対座標（メートル）
  → 図郭原点Y(m) + 座標値 / coordUnitDivisor
  → jpRectToWgs84(abs_x, abs_y, zone)  // 平面直角 → WGS84
  → [lon, lat]
```

`coordUnitDivisor` は図郭座標行の座標値単位フィールドから決定されます（下記参照）。

### 図郭原点と座標値単位の取得

M レコード直後の行（図郭座標行）から左下座標と座標値単位を取得します。
拡張DMの付属行には旧DMの `MB` プレフィックスがないため、フィールド位置が異なります。

#### 旧DM（`MB` レコード）

```
位置     長さ  内容
[1-2]    2    "MB" 固定
[3-9]    7    左下X (m)
[10-16]  7    左下Y (m)
[17-19]  3    座標値単位
[20-26]  7    要素数
[27-32]  6    レコード数
[33-39]  7    右上X (m)
[40-46]  7    右上Y (m)
```

#### 拡張DM（M レコード直後の付属行）

```
位置     長さ  内容
[1-7]    7    左下X (m)
[8-14]   7    左下Y (m)
[15-17]  3    座標値単位
  ...          (要素数、レコード数、右上座標 等)
```

**座標値単位と除算器の対応**:

| 座標値単位 | 単位 | 除算器(coordUnitDivisor) | 主な地図情報レベル |
|------------|------|--------------------------|--------------------|
| 1          | mm   | 1000                     | 500, 1000          |
| 10         | cm   | 100                      | 2500, 5000         |
| 1000       | m    | 1                        | 10000              |

**例**: `" 102000 -24000 108000 -16000    74146 163047 10 ..."`

- 左下X=102000m、左下Y=-24000m
- 座標値単位=10(cm) → coordUnitDivisor=100
- 図郭範囲: X方向6000m、Y方向8000m

## 平面直角座標系 → WGS84 変換

TM（横メルカトル）逆変換を使用。Bowring/Snyder 方式で GRS80 楕円体パラメータに基づきます。

### 系番号と原点

| 系 | 原点緯度 | 原点経度 | 主な地域 |
|----|----------|----------|----------|
| 1 | 33°00′ | 129°30′ | 長崎・鹿児島西部 |
| 2 | 33°00′ | 131°00′ | 福岡・佐賀・大分・熊本 |
| 3 | 36°00′ | 132°10′ | 山口・島根・広島 |
| ... | ... | ... | ... |
| 9 | 36°00′ | 139°50′ | 東京・神奈川・埼玉・千葉 |

スケールファクター: 0.9999

## 分類コード

主要な分類コード（4桁、上2桁 = 大分類、下2桁 = 小分類）:

| コード | 分類名 |
|--------|--------|
| 0101 | 基準点（三角点） |
| 0301 | 真幅道路 |
| 0501 | 普通建物 |
| 0502 | 堅ろう建物 |
| 0703 | 河川 |
| 1001 | 等高線 |
| 1100 | 注記 |

## 使用方法

```typescript
import { convertDMFileToGeoJSON } from './dm';

// ブラウザの File API から直接変換
const geojson = await convertDMFileToGeoJSON(file, {
  zoneNumber: 2,  // 平面直角座標系の系番号（省略時: 拡張DM=2, 旧DM=9）
});

// テキストから変換
import { convertDMtoGeoJSON, decodeShiftJIS } from './dm';
const buffer = await file.arrayBuffer();
const text = decodeShiftJIS(buffer);
const geojson = convertDMtoGeoJSON(text, {
  zoneNumber: 2,
  includeAnnotations: true,   // 注記をフィーチャとして含めるか（default: true）
  coordinatePrecision: 8,     // 座標の小数点以下桁数（default: 8）
});
```

### ConvertOptions

| オプション | 型 | デフォルト | 説明 |
|------------|-----|-----------|------|
| `zoneNumber` | `number` | 拡張DM: 2, 旧DM: 9 | 平面直角座標系の系番号 (1〜19) |
| `coordTransformer` | `CoordTransformer` | 内蔵変換 | カスタム座標変換関数（proj4js等） |
| `includeAnnotations` | `boolean` | `true` | 注記フィーチャの出力 |
| `coordinatePrecision` | `number` | `8` | 座標の丸め精度 |

## 検証済みデータ

| ファイル | フォーマット | 系番号 | フィーチャ数 | 座標数 | エリア |
|----------|-------------|--------|-------------|--------|--------|
| `401005_kihonzu_dmdata100_15.dm` | 拡張DM | 2 | 22,288 | 39,584 | 北九州市（門司区付近） |

## 参考資料

- [公共測量 作業規程の準則 付録7「公共測量標準図式」](https://psgsv2.gsi.go.jp/koukyou/jyunsoku/index.html) — 公式仕様書
- [DMデータの仕様](https://takamoto.biz/2020/06/%E9%83%BD%E5%B8%82%E8%A8%88%E7%94%BB%EF%BD%87%EF%BD%89%EF%BD%93%E3%81%AE%E6%A7%8B%E7%AF%89%EF%BC%92%E2%88%92dm%E3%83%87%E3%83%BC%E3%82%BF%E3%81%AE%E4%BB%95%E6%A7%98/) — DMデータ概要解説
- [DMデータの変換](https://takamoto.biz/2020/06/%E9%83%BD%E5%B8%82%E8%A8%88%E7%94%BB%EF%BD%87%EF%BD%89%EF%BD%93%E3%81%AE%E6%A7%8B%E7%AF%89%EF%BC%93%E2%88%92dm%E3%83%87%E3%83%BC%E3%82%BF%E3%81%AE%E5%A4%89%E6%8F%9B/) — 座標変換の実装例
- [数値地形図データファイル仕様（林野庁）](https://www.rinya.maff.go.jp/kanto/apply/publicsale/kanri/pdf/furoku8-1.pdf)
